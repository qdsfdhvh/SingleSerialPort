buildscript {
    apply from: 'versions.gradle'
    addRepos(repositories)
    dependencies {
        classpath deps.android.gradle_plugin
        classpath deps.kotlin.gradle_plugin
//        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.8.4'
//        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1'
        classpath 'com.novoda:bintray-release:0.9.1'
    }
}

allprojects {
    addRepos(repositories)
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

//tasks.getByPath(":serial-modbus:androidJavadocs").enabled = false
//tasks.getByPath(":serial-rs232:androidJavadocs").enabled = false
//tasks.getByPath(":serial-target:androidJavadocs").enabled = false
//tasks.getByPath(":serial-usb:androidJavadocs").enabled = false